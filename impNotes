=> 1) debugger is like a breakpoint in javaScript
=> 2) fix values are called literals and variable values are called variables
=> 3) javaScript is case sensitive
  var firstName;
  var firstname:
    above are two different variables
=> 4) 
    *) pehle string agaya to sab ko concate kardega 
    var x = "2" + 5 + 5;
    document.write(x)
    //output 255
    *) bad me string ayega to pehle calculation karega then cancat karega
    var x = 5 + 5 + "2";
    document.write(x)
    //output 102
=> 5) ** (exponential) this is use for power
      var x = 3**3 //(3 power 3)
      document.write(x)
      //output 27
=> 6) javaScript data types are dynamic
      var x = 5;
      x = "javed";
=> 7) long values likhne ka tareeka
      var num = 125e5
=> 8) javaScript objects cannot be compared      
      var x = new String("John");  // x is an object
      var y = new String("John");  // y is an object
      document.getElementById("demo").innerHTML = (x==y); //return false
      

